<Window x:Class="T4Studio.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:T4Studio.Client"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="T4 Forge - Studio"
        Height="800"
        Width="1000"
        Style="{StaticResource MaterialDesignWindow}"
        
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto">
    <Grid Margin="4px 8px">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Menu Grid.Row="0" Grid.ColumnSpan="3" Style="{StaticResource MaterialDesignMenu}">
            <MenuItem Header="_Datei">
                <MenuItem Header="_Laden" Click="LoadFile_Click" />
                <MenuItem Header="_Speichern" Click="SaveFile_Click" InputGestureText="Ctrl+S" />
                <Separator />
                <MenuItem Header="_Beenden" Click="Exit_Click" />
            </MenuItem>

            <MenuItem Header="_Debug">
                <MenuItem Header="Test: Einfacher Loop" Click="LoadDebugTemplate_Click" Tag="Loop" />
                <MenuItem Header="Test: POCO Klasse" Click="LoadDebugTemplate_Click" Tag="Poco" />
                <MenuItem Header="Test: Fehler-Stresstest" Click="LoadDebugTemplate_Click" Tag="Error" />
            </MenuItem>
            
            <MenuItem Header="_Switch Theme" Click="SwitchEditorTheme"></MenuItem>

            <MenuItem Header="_About">
                <MenuItem Header="Info" />
                <MenuItem Header="Changelog" />
            </MenuItem>

        </Menu>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3">
            <ToolBar>
                <ToolBar.Header>Snippets</ToolBar.Header>
                <Button Style="{StaticResource MaterialDesignFlatButton}" Margin="5px 0" Tag="Dir" Click="InsertSnippet_Click" ToolTip="T4 Template Directive">
                    <materialDesign:PackIcon Kind="Xml" />
                </Button>

                <Button Style="{StaticResource MaterialDesignFlatButton}" Tag="Loop" Click="InsertSnippet_Click" ToolTip="For-Schleife (Umschließen)">
                    <materialDesign:PackIcon Kind="Repeat" />
                </Button>

                <Button Style="{StaticResource MaterialDesignFlatButton}" Tag="If" Click="InsertSnippet_Click" ToolTip="If-Bedingung (Umschließen)">
                    <materialDesign:PackIcon Kind="CodeBraces" />
                </Button>

                <Button Style="{StaticResource MaterialDesignFlatButton}" Tag="Expression" Click="InsertSnippet_Click" ToolTip="Ausdruck">
                    <materialDesign:PackIcon Kind="CodeEqual" />
                </Button>

                <Button Style="{StaticResource MaterialDesignFlatButton}" Tag="Comment" Click="InsertSnippet_Click" ToolTip="T4 Kommentar">
                    <materialDesign:PackIcon Kind="CommentText" />
                </Button>
            </ToolBar>
            <ToolBar>
                <ToolBar.Header>Objects</ToolBar.Header>
                <Button Style="{StaticResource MaterialDesignFlatButton}" Tag="Prop" Click="InsertSnippet_Click" ToolTip="C# Property">
                    <materialDesign:PackIcon Kind="AlphaPBox" />
                </Button>

                <Button Style="{StaticResource MaterialDesignFlatButton}" Tag="Date" Click="InsertSnippet_Click" ToolTip="Datum">
                    <materialDesign:PackIcon Kind="CalendarClock" />
                </Button>
            </ToolBar>
        </StackPanel>

        <materialDesign:Card Grid.Row="2" Grid.Column="0" Margin="4" UniformCornerRadius="4" VerticalAlignment="Stretch">
            <avalonEdit:TextEditor
                x:Name="EditorInput"
                Grid.Column="1"
                ShowLineNumbers="True"
                SyntaxHighlighting="C#"
                TextChanged="EditorInput_TextChanged"
                Background="LightGray"
                Foreground="{DynamicResource MaterialDesignBody}" />
        </materialDesign:Card>

        <GridSplitter Grid.Row="2" Grid.Column="1" HorizontalAlignment="Center" Width="4" Background="Transparent"/>

        <materialDesign:Card Grid.Row="2" Grid.Column="2" Margin="4" UniformCornerRadius="4" Background="{DynamicResource MaterialDesignBackground}">
            <avalonEdit:TextEditor
                x:Name="EditorOutput"
                IsReadOnly="True"
                ShowLineNumbers="True"
                SyntaxHighlighting="C#"
                Background="LightGray"
                Foreground="{DynamicResource MaterialDesignBody}" />
        </materialDesign:Card>

        <GridSplitter Grid.Row="3" Grid.ColumnSpan="3" Height="4" HorizontalAlignment="Stretch" Background="Transparent"/>

        <materialDesign:Card Grid.Row="4" Grid.ColumnSpan="3" Margin="4" Background="{DynamicResource MaterialDesignCardBackground}">
            <ListView 
                x:Name="ErrorList"                 
                Style="{StaticResource MaterialDesignListView}"
                SelectionMode="Single"
                MouseDoubleClick="ErrorList_MouseDoubleClick">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Zeile" DisplayMemberBinding="{Binding Line}" />
                        <GridViewColumn Header="Spalte" DisplayMemberBinding="{Binding Column}" />
                        <GridViewColumn Header="Beschreibung" DisplayMemberBinding="{Binding ErrorText}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </materialDesign:Card>

        <StatusBar Grid.Row="5" Grid.ColumnSpan="3" Background="{DynamicResource PrimaryHueDarkBrush}" Foreground="White">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Bereit" />
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>
